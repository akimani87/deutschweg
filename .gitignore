# Environment — NEVER commit API keys
.env

# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# NOTE: supabase-config.js is SAFE to commit.
# It contains only the public anon key, which is designed for client-side use.
# The service role key (if you ever use it) must NEVER be committed.
# Add it to .env on the server side only.
